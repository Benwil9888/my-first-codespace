#include<stdio.h>
int main(){
int length,width,area;
printf("enter length");
scanf("%d",&length);
printf("enter width");
scanf("%d",&width);
int area=length*width;
printf("area of rectangle is %d",area);
return 0;
}   